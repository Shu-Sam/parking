<nav class="navbar navbar-expand-lg navbar-primary bg-primary">
  <div class="container-fluid">
    <div class=navbar-brand">
      <%= link_to image_tag("nav_parking_sign.jpeg", size: "40x40"), root_path, title: 'Home page' %>
    </div>

    <div class="navbar-nav">
      <div class="nav-item container-fluid">
        <% if user_signed_in? %>
          <%= current_user.name.capitalize %>_<%=current_user.role %>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-sm btn-danger' %>
        <% else %>
            <%= link_to 'Log in', new_session_path(resource_name), class: 'btn btn-warning btn-sm' %>
            <%= link_to 'Sign up', new_registration_path(resource_name), class: 'btn btn-warning btn-sm' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
