<div class="container mt-lg-3">
  <div class="row">
    <div class="col col-md-10">
      <div class="row justify-content-center text-center text-wrap fs-3 fw-bold">
          Current Reservations
      </div>

      <%= render partial: 'reservations/reservation', collection: @current_reservations&.includes([:car_park]) %>

      <div class="row justify-content-center text-center text-wrap fs-3 fw-bold">
        Past Reservations
      </div>
      <%= render partial: 'reservations/reservation', collection: @past_reservations&.includes([:car_park]) %>
    </div>
  </div>
</div>




